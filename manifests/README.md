# Manifests

Committed manifests should be small curated release pointers only. Runtime
manifests and checkpoints belong under ignored paths:

- `manifests/runs/`
- `manifests/checkpoints/`
- the run-specific output root configured in `config/fire_vase_pipeline.yml`
