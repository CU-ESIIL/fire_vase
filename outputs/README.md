# Outputs Directory

This directory may contain small curated figures for documentation, but bulk
analysis products should be written to ignored roots such as `scratch/`,
`artifacts/`, `lakehouse/`, or `outputs/fire_vase_lakehouse/`.
